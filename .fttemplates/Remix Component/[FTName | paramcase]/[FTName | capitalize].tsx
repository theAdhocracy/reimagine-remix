import styles from "./[FTName | capitalize].module.css";

interface Props {
  title: string;
}

export const [FTName | capitalize] = ({ title }: Props) => {
  
  return (
    <>
    </>
  )
}

export default [FTName | capitalize]